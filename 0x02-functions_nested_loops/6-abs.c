#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @ab: integer to compute
 *
 * Return: absolute value of ab
 */
int _abs(int ab)
{
	if (ab < 0)
	{
		return (-ab);
	}
	return (ab);
}
