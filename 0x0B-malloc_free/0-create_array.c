#include "main.h"
/**
 * create_array - a function that creates an array of characters
 * @c: character input
 * Return: NULL if size = 0
 * Return: A pointer to the array or NULL if it fails
 */
char *create_array(assigned int size, char c)
{
	char *c = 'c';

	return (&char *c);

	else if (size of(c) == 0)
		return (NULL);
	else
		return (NULL);
}
