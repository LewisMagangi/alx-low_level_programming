#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: a character input
 * Return: length of a string
 */
int _strlen(char *s)
{
	for (char i = *s; i != '\0'; i++)
	{
		_strlen = 0;

		_strlen++;
	}
}
