#include "main.h"

/**
 * mul - a function that multiplies two digits
 * @a: first interger input
 * @b: second interger input
 *
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (0);
}
