#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: a character input
 * Return: length of a string
 */
int _strlen(char *s)
{
	char x[];
	char *s = x;

	for (i = 0; x[i] != '\0'; i++)
	{
		_strlen = i;
	}
	return (_strlen);
}
