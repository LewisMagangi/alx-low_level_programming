#include "main.h"

/**
 * _isupper - a function that checks if c is uppercase
 * @c: single letter input
 * Return: 1 if c is uppercase, 0 if otherwise
 */
int _isupper(int c)
{
	if ((c >= 'A') && ((C <= 'Z'))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
