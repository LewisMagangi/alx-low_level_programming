#include "main.h"

/**
 * _islower - checks if c is lowercase 
 * @c: single letter input
 * Returns: 1 if c is lowercase else returns 1
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}

