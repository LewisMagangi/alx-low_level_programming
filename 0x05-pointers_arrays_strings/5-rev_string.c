#include "main.h"
/**
 * rev_string - a function that reverses a string
 * @s: A character input
 */
void rev_string(char *s)
{
	int j = 0;
	int i, n;

	for (i = 0; s[i] != '\0'; i++)
	{
		j++;
	}
	for (n = j - 1; n >= 0; n--)
	{
		char *s = s[n];
	}
}
